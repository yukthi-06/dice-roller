# Prompt
since the final words are in lower case, make the gibberish words as lowercase too

# Implementation
Updated `getRandomGibberishWord()` inside `RandomUtils.java`. Removed the uppercase branch so that all procedurally generated intermediate characters are strictly lowercase, visually matching the case of the final dictionary strings.
