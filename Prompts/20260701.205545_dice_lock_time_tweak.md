# Prompt
the number has to lock a litle bit time more

# Implementation
Adjusted the termination condition in the `animationRunnable` loop inside `DiceFragment.java` to increase the final face visibility duration.
Changed the logic from stopping `400ms` early to stopping exactly halfway (`duration / 2`) through the overall animation.
Now, the visual face shuffling executes rapidly during the first half of the tumbling animation, but locks into the final outcome for the entire second half, giving the user significantly more time to perceive the correct number while the rotation spins down.
