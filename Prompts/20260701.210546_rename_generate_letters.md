# Prompt
rename 'Generate Alphabet' to 'Generate Letters'

# Implementation
Renamed the `action_alphabet` string resource inside `strings.xml` from 'Generate Alphabet' to 'Generate Letters'. The Alphabet screen's primary button now reads 'Generate Letters'.
