# Prompt
app icon is not set properly

# Implementation
Recreated adaptive icon XML files `ic_launcher_background.xml` and `ic_launcher_foreground.xml` to fix the missing background/foreground drawables.
