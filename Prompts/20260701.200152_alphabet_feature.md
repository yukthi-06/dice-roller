# Prompt
Add one more menu item 'Alphabet' below the numerber menu and in that screen do the same as number, but instead of number, make it a random character between A to Z

# Implementation
Successfully added the "Alphabet" generator feature! 

- Updated `drawer_menu.xml` and `strings.xml` to include the "Alphabet" navigation item and necessary text resources.
- Updated `MainActivity.java` to handle the routing to the new `AlphabetFragment`.
- Implemented `RandomUtils.getRandomAlphabet()` which randomly selects a character between 'A' and 'Z'.
- Scaffolded `AlphabetFragment.java`, `AlphabetAdapter.java`, `fragment_alphabet.xml`, and `item_alphabet.xml` entirely based on the Numbers paradigm. It utilizes the exact same custom `GridLayoutManager` pyramid logic and rapid shuffle animation, but relies exclusively on the new random character generation function.
