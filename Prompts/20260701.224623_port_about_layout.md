# Prompt
Copy About page layout from 'X:\Projects_X\0_Active\8_Android_APK\02_Stable\TODO_Tasks_List_APK_GITHUB'

# Implementation
Successfully ported the highly polished About page layout from the `TODO_Tasks_List_APK_GITHUB` project over to the current app.

- **Layout File**: Migrated `activity_about.xml` from the source project, wrapping it into `fragment_about.xml` for this project. Stripped out the `AppBarLayout`/`Toolbar` (since `MainActivity` handles the toolbar locally), keeping the core `NestedScrollView` and `MaterialCardView` layout structure. Replaced hardcoded custom colors with standard Android theme attributes (`?android:attr/textColorPrimary`, etc.) so it immediately respects Light/Dark modes natively.
- **Dynamic Build Info**: Enabled `buildConfig true` in `app/build.gradle` and hooked `AboutFragment.java` to dynamically populate the `tvBuildInfo` field with the true App Version, Version Code, Application ID, and Build Type at runtime.
