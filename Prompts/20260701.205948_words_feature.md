# Prompt
Add one moe menu items 'Words'.  This shoul dwork like the alphabet screen, but it should show 1 to 10 words from a dictionary(not too difficult to pronounce)

# Implementation
Successfully implemented the new **Words** generator feature!

- **Dictionary Setup**: Created a static array of 60 highly recognizable, easy-to-pronounce dictionary words (e.g. "Apple", "Elephant", "Kangaroo", "Xylophone", "Zebra", etc.) within `RandomUtils.java`. Added a `getRandomWord()` function to fetch them.
- **UI Elements**: Designed `item_word.xml` to be slightly wider than the Alphabet card (`110dp` wide x `60dp` high) to accommodate full words, and utilized Android's `autoSizeTextType` to ensure longer words seamlessly shrink to fit without overflowing.
- **Scaffolding**: Created `WordsFragment.java`, `WordAdapter.java`, and `fragment_words.xml`.
- **Navigation**: Added the "Words" entry to `drawer_menu.xml` and handled routing in `MainActivity.java`.
