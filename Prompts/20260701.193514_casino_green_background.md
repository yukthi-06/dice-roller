# Prompt
make the background same a casino table green color

# Implementation
Added `casino_green` (#0E5E26) and `dark_casino_green` (#0A421A) to `colors.xml`.
Updated both day and night `themes.xml` files to apply these colors to `android:windowBackground` and `android:colorBackground`. Ensure that text fields contrast perfectly using white text values.
