# Prompt
roll back the blur effects

# Implementation
Reverted the visual motion blur effect implemented on the Words screen by:
1. Removing the `isBlurring` flag and `setBlurring()` API from `WordAdapter.java`.
2. Stripping out the `RenderEffect` and alpha transparency code from `onBindViewHolder`.
3. Removing the state toggles from the `generateWords()` animation runnable in `WordsFragment.java`.
The generation sequence now functions precisely as it did before, flashing fully opaque words.
