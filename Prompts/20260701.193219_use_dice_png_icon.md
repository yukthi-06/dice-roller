# Prompt
use 'dice.png' found in the project root folder as the app icon

# Implementation
Modified `app/build.gradle` to automatically copy `dice.png` into `mipmap-xxhdpi/ic_launcher.png` and `ic_launcher_round.png` during Gradle evaluation, while simultaneously deleting the `anydpi-v26` folder to ensure proper fallback rendering without requiring terminal access.
